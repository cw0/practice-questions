const strStr = (haystack, needle) => {
  if (haystack.length === 0 || haystack.length < needle.length) return 0;
  
  let i = 0;
  let j = 0;
  while (i < haystack.length) {
    if (haystack[i])
  }
};